name: examples
description: svg_to_path_generator_example
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  svg_to_path_annotation:
    path: ../../svg_to_path_annotation

dev_dependencies:
  flutter_test:
    sdk: flutter
  integration_test:
    sdk: flutter
  build_runner: ^1.11.0
  svg_to_path_generator:
    path: ../../svg_to_path_generator

flutter:
  uses-material-design: true